<h2>Users</h2>

<table>
  <tr>
    <th>Title</th>
  </tr>

<% for user in @users %>
  <tr>
    <td><%= h  user.login %></td>
    <td><%= link_to 'Edit', edit_admin_user_path(user) %> | <%= link_to 'Destroy', admin_user_path(user), :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New user', new_admin_user_path %>
