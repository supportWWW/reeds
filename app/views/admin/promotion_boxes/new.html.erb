<h2>New promotion box</h2>

<% form_for :promotion_box, :url => admin_promotion_boxes_path do |f| -%>

	<%= render :partial =>  "form", :locals => { :f => f } %>
	<p>
	  <%= f.submit "Create" %>
	</p>

<% end %>

<%= link_to 'Back', admin_promotion_boxes_path %>